package com.socion.session.repository;

import com.socion.session.dao.Failedattestation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FailedattestationRepository extends JpaRepository<Failedattestation, Long> {
}
